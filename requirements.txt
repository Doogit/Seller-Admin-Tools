-c constraints.txt
pandas==3.0.3
PyYAML==6.0.3
python-pptx==1.0.2
pytest==9.0.3
# Pin is load-bearing: 0.14.11's fast_app(default_hdrs=False, pico=False) emits
# no external CDN header (offline invariant). See web/server.py startup assert.
python-fasthtml==0.14.11
# Tailwind CSS is built by the standalone CLI (v3.4.17, not a pip dep); the
# committed web/static/tailwind.css keeps a fresh clone offline. See Makefile.
