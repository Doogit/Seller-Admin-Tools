"""View models: pure presentation logic (formatting + user-facing strings) over
the analytical core. Additive — no existing core module is modified. Kept free of
web/Streamlit imports so they stay unit-testable headlessly and shared by any UI.
"""
